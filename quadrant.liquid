<div class="screen">
  <div class="view view--quadrant">
    <div class="layout layout--col layout--top layout--center-x">

      <!-- PERCENTAGE -->
      <div class="value value--large">
        {{ urineTankPercentage }}%
      </div>

      <!-- LABEL -->
      <div class="label mt text--gray-30">
        ISS URINE TANK LEVEL
      </div>

      <!-- PROGRESS BAR -->
      <div class="progress-bar progress-bar--small mt w--full">
        <div class="track">
          <div class="fill" style="width:{{ urineTankPercentage }}%">
          </div>
        </div>
      </div>

    </div>
  </div>
</div>
